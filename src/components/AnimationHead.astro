---
// this file is added to the detaillayout to automatically add css stylesheets for the animation in question
const { slug } = Astro.props;
---

<!-- adds a link to a stylesheet for a specific animation -->
{slug && (
  <link rel="stylesheet" href={`/styles/animation-css/${slug}.css`} />
)}
