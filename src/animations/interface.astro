---

---

<div class="interface-container">
    <div class="interface-button-container set-sibling-index">
        <button><span>I</span></button>
        <button><span>N</span></button>
        <button><span>T</span></button>
        <button><span>E</span></button>
        <button><span>R</span></button>
        <button><span>F</span></button>
        <button><span>A</span></button>
        <button><span>C</span></button>
        <button><span>E</span></button>
    </div>

    <p class="small-interface span-split set-sibling-index">Interface</p>

    <div class="interface-button-container set-sibling-index">
        <button><span>I</span></button>
        <button><span>N</span></button>
        <button><span>T</span></button>
        <button><span>E</span></button>
        <button><span>R</span></button>
        <button><span>F</span></button>
        <button><span>A</span></button>
        <button><span>C</span></button>
        <button><span>E</span></button>
    </div>
</div>



<script>
	// @ts-nocheck
	// =====================================
	// MARK: Animation Pause button
	// =====================================

	const animationButton = document.querySelector(".animation-button");
	const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
	let animationPlaying;

	// check if the user has motion preferences and set the according class on the button
	if(prefersReducedMotion === true) {
		animationButton?.classList.add("stop-animation");
		animationPlaying = false;
	} else {
		animationButton?.classList.add("play-animation");
		animationPlaying = true;
	}

	// toggle animation
	function toggleAnimation() {
		animationButton.classList.toggle("stop-animation");
		animationButton.classList.toggle("play-animation");

		let animationClass = document.querySelector(".stop-animation");

		animationClass ? animationPlaying = false : animationPlaying = true;

		// use this if ever implemented in general js
		// if (typeof onAnimationStateChange === "function") {
		// 	onAnimationStateChange(animationPlaying);
		// }
	}

	animationButton?.addEventListener("click", toggleAnimation)

</script>

<script>
    const delay = 1;  // seconds, same as --delay
    const count = 9;    // number of letters
    const cycleMs = count * delay * 1000;

    function restartAnimations() {
        const animatedEls = document.querySelectorAll(
        '.interface-button-container button span, .small-interface span'
        );

        animatedEls.forEach(el => {
        el.classList.remove('animate');
        void el.offsetWidth;      // force reflow
        el.classList.add('animate');
        });
    }

    // add the class in CSS and JS uses it to retrigger the animations
    // .animate { animation-name: typingKeys / revealLetter; }

    restartAnimations();
    setInterval(restartAnimations, cycleMs);
</script>



